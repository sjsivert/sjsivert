/**
 * The Sanity types for all Global items/components
 * (the _types used by the serializer)
 */
export enum GlobalComponentType {
    GLOBAL_COMP_INFO_BOX = "globalCompInfoBox",
    GLOBAL_COMP_ALERT = "globalCompAlert",
}
